<div class="matchright">
<h1> <%=@team.name%> </h1>
League: <%=link_to @team.league.name, @team.league,{:style=>'color:#00FFFF;', :class => "css_class"}%><br>
<P ...>
</div>
<div class="bigleft">
  <table>
    <tr>
      <th> Name </th>
      <th> Position </th>
    </tr>
<% @team.players.each do |p| %>
<tr>
<th><%= link_to p.name, player_path(p)%></th>
<th><%=p.position%></th>
</tr>
<% end %>
</table>
</div>
